# TASK-009-01: Electron 适配 — MCP 端口环境变量化

## 父任务
TASK-009

## 涉及文件
- `electron/mcp/server.ts` — MCP_PORT 改为从 `MCP_PORT` 环境变量读取，默认 3399
- `electron/main.ts` — 无改动（确认不受影响）

## 验收标准
1. `MCP_PORT` 常量改为 `parseInt(process.env.MCP_PORT ?? "3399", 10)`
2. 启动日志输出实际使用的端口
3. 不传环境变量时行为与现在完全一致（3399）
4. esbuild 构建通过

## 方案要点
- 仅改 `electron/mcp/server.ts` 第 58 行的 MCP_PORT 常量
- 最小改动，不影响现有行为
